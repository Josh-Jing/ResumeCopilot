[project]
name = "resume-copilot-backend"
version = "0.2.0"
description = "ResumeCopilot FastAPI backend"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.136.3",
    "playwright>=1.60.0",
    "uvicorn[standard]>=0.49.0",
    "watchdog>=6.0.0",
]
